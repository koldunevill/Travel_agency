<Page x:Class="Travel_agency.AddEditTravelPackagePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Add or Edit Travel Package">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Поля ввода данных путёвки -->
        <!--<StackPanel Grid.Row="0" Margin="20">
            <TextBlock Text="Название путевки:" />
            <TextBox Name="PackageNameTextBox" Width="200"/>

            <TextBlock Text="Дата заезда:" />
            <DatePicker Name="ArrivalDatePicker" Width="200"/>

            <TextBlock Text="Дата отъезда:" />
            <DatePicker Name="DepartureDatePicker" Width="200"/>

            <TextBlock Text="Цена:" />
            <TextBox Name="PriceTextBox" Width="200"/>

            <Button Content="Сохранить" Name="SaveButton" Width="100" Height="40" Click="SaveButton_Click" Margin="10"/>
        </StackPanel>-->
    </Grid>
</Page>
